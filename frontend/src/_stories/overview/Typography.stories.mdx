import { Meta, Typeset, Story, Preview, Props } from "@storybook/addon-docs/blocks";

import typography from "theme/typography";

import { H3, H4, H5, H6, Subtitle1, Subtitle2, Body, Body2, Caption, CenterCaption } from "widgets/Label";
import { primaryColor, getTheme } from "theme/theme";
import { ColorfulTypeset } from "../ColorfulTypeset";

export const sampleText = "Kubernetes Application Manager";
export const palette = getTheme("light").palette;

<Meta title="Design System/Typography" />

# Typography

**Font:** Roboto

---

**Text Color:** `colors.textBlackHighEmphasis`

**Background Color:** `colors.surfacePage`

<Story name="blackDisabledEmphasis in surfacePage">
  <ColorfulTypeset
    foregroundColor={palette.text.primary.main}
    backgroundColor={palette.background.default}
    labels={[H3, H4, H5, H6, Subtitle1, Subtitle2, Body, Body2, Caption]}
    sampleText={sampleText}
  />
</Story>

---

**Foreground:** Font Color: `colors.textWhiteHighEmphasis`

**Background:** primary color

<Story name="whiteHighEmphasis in primary">
  <ColorfulTypeset
    foregroundColor={palette.primary.contrastText}
    backgroundColor={palette.primary.main}
    labels={[H3, H4, H5, H6, Subtitle1, Subtitle2, Body, Body2, Caption]}
    sampleText={sampleText}
  />
</Story>
