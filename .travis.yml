branches:
  only:
    - ci

git:
  depth: false

jobs:
  include:
    - go: 1.12
      script:
        - ls
      cache:
        directories:
          - $HOME/.cache/go-build
          - $HOME/gopath/pkg/mod
